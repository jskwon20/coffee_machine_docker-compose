apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - mysql.yaml
  - order.yaml
  - inventory.yaml
  - billing.yaml
  - frontend.yaml
  - ingress.yaml
  - ingress-class.yaml
  - ingress-params.yaml

configurations:
  - kustomizeconfig.yaml

